/**
 * gestion du corps de la page
 */
/*window.onload = function initializeBody(){
	var menu = document.getElementById('left_menu');
	var menuRect = menu.getBoundingClientRect();
	var centerContainer = document.getElementById('center_container');
	centerContainer.style.position = "absolute";
	centerContainer.style.left = menuRect.right;
}*/